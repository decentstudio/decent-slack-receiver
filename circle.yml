machine:
  environment:
    RABBITMQ_HOST: localhost
    RABBITMQ_PORT: 5672
    RABBITMQ_USER: guest
    RABBITMQ_PASS: guest
    RABBITMQ_VHOST: /
  services:
    rabbitmq-server
deployment:
  production:
    branch: fix-ci
    commands: ./docker-push.sh
